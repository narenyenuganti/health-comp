# Competition large-text layout repair

## Latest checkpoint — actual result repair

The sections below retain the diagnostic chronology; statements that the result source is unchanged describe their historical checkpoint. The actual result card now uses the result-specific width-fitting native layout described in the final implementation sequence. The unchanged unfiltered Dynamic Type audit and both hosted geometry tests passed together in `ResultLayoutFocusedRepair.xcresult` (3/3). Two temporary complete-result XXXL/accessibility5 capture tests passed (2/2), including gap-free overlap and unfiltered clipping checks after every capture. Main-agent inspection of ordinary, XXXL, and both accessibility5 segments confirmed complete names, scores, units, compact/stacked presentation, and appropriate wrapping.

All temporary tests have been removed; twenty-two permanent UI tests remain. The six-file source/test diff is `0ba396b9f56750199a75ffea28b49f8362cf4905220bc59c99b941fee4b04c4c`. The cleaned-source `FullUIAfterResultRepair.xcresult` completed **22 passed, 0 failed/skipped**, in 1,001 seconds, including the original unfiltered audit and all twenty-eight appearance checkpoints. Independent combined review found no Critical/Important source or test finding; its Minor evidence-timeline clarification is addressed by this current checkpoint and independently cleared. Candidate secret scanning passed using an isolated index without changing the real index.

All application tests passed **548/548** in `PostLargeTextAllAppTests.xcresult`. Unsigned Debug, Staging, and Release device builds passed serially. Staging/Release property lists lint cleanly and their executables omit the checked DEBUG fixture markers; the Debug binary contains the positive-control marker. These are unsigned build checks, not physical installation or production configuration evidence. Backend, CompetitionCore, project generation, resources, and package-resolution inputs are unchanged from exact base `23710522`, whose backend and iOS CI runs `33922485571` / `33922485471` were reread as successful. This is explicit unchanged-input evidence reuse, not execution on the new source.

Commit the reviewed repair separately from existing documentation dependency `23710522`, and submit one main-targeted PR retaining that commit as an ancestor. Both exact-new-head CI jobs must pass before a head-pinned merge commit, followed by exact-main CI and dependency reachability/readback. Do not squash away the documentation ancestry or mutate the dirty original main checkout. The broader production Goal remains incomplete; the selected phone artifact stays unchanged.

## Why this is a product fix, not an audit exception

Task 14's expanded deterministic UI qualification exposed genuine shared-renderer defects. At ordinary XXXL text size, the seven-column chart truncates the active owner's score to `B 4...`. At accessibility5, the fixed-width day column splits `Day 3` into fragments and the remote participant rows display hard-coded `NY` / `A` markers instead of identifying their real fixture participants. Spoken day labels already name both participants; preserve those contracts while repairing the visual presentation.

Reproduction: source `23710522fa11e1798fbb4be83cb6704abdd6562e`, with DEBUG environment metadata and fixture-only diagnostic tests, on iPhone 16 Pro / iOS 18.4. `RemoteChartSizeDiagnostic.xcresult` captured four actual, asserted categories. Its capture-only pass is not an accessibility pass. The XXXL image is `ChartSizeAttachments/7F642FEE-CA51-416D-9D01-A17611CB4731.png`; the accessibility5 image is `ChartSizeAttachments/EE7F184D-3BB0-4282-8315-7B0989C0A322.png`. Both live under `/private/tmp/healthcomp-debug-ui.sb5pvp`. Repeated pinned and unpinned XS clipping audits also failed; neither is exempted.

Durable synthetic screenshots are also retained in the continuation workspace at `outputs/takeover-2026-09-04/ui-qualification/remote-chart-xxxlarge-before.png` and `remote-day-rows-accessibility5-before.png`. The full twenty-test prerequisite baseline (`FullUIBeforeLayoutFix.xcresult`) completed on September 4 at 23:49 UTC: eighteen passed, two failed, zero skipped. Both original dark-mode harness failures passed; the expanded remote clipping and Dynamic Type audits remained RED. The package lock was unchanged.

## Boundary

Work in `bugfix/competition-large-text-layout`, starting at `2371052`. Preserve the existing uncommitted qualification worktree and unrelated package-lock changes. No authentication, server, database, HealthKit, scoring, command, invitation, persistence, signing, or selected physical artifact changes. No physical phone, production action, password, package update, or new runtime is needed. The selected installed phone artifact remains unchanged until a later separately bounded installation.

The test-only qualification plan did not authorize silently editing production views to satisfy assertions. This dedicated repair plan records the now-proven product defect and keeps the fix independently reviewable.

## Design requirements

- Preserve the compact seven-day comparison when its full text fits. Use an adaptive day presentation when it does not; never shrink Dynamic Type or truncate score text to force the chart to fit.
- Remove the rigid 62-point day-label constraint at large sizes. Day/date and both participant scores must remain fully readable and scroll-reachable, including accessibility5.
- Derive visible participant identity from the supplied owner/opponent names. Keep the existing complete spoken day labels, stable day identifiers, current/completed/upcoming distinctions, and unavailable-versus-zero semantics.
- Keep the visible `Today` cue on the current day when ordinary sizes fall back to stacked rows; retain the date and unchanged combined spoken label.
- Reuse the same shared renderer for local and remote competitions. Avoid a separate remote-only layout or changed domain model.
- Use `ViewThatFits(in: .horizontal)` for ordinary text sizes, measuring the compact chart with intrinsic, unbroken horizontal text. Expand the surrounding card outside that fitting decision. Preserve explicit accessibility-size rows; the fallback stacks full-width day/date and named participant blocks rather than squeezing three columns. Do not add fixed category thresholds, hard-coded initials, or a new score format.
- Keep the DEBUG Test Lab warning separate from product layout. Earlier ideal-height and identifier-removal controls were reverted. Independent review accepted retaining the body font for warning legibility, with its larger fixture viewport footprint acknowledged. Both the old caption2 and body fonts are native text styles; this is not a production fixed-font repair.
- Keep result-screen diagnosis separate. Different unfiltered runs identified the hidden decorative dash, scores, names, and units. Actual large/XXXL/AX5 captures show growing text and the existing horizontal-to-vertical layout working. Neither the proposed separator replacement nor a speculative result-layout change is applied. Sequential and isolated-result audits both fail, and the same warning reproduces on iOS18.4 and iOS26.4.1. These observations do not establish a product fixed-font defect or justify an audit exemption.

## RED-to-GREEN sequence

1. Transfer only the reviewed prerequisite test-harness changes into this worktree, preserving their original evidence and keeping temporary diagnostics out of the permanent suite.
2. Add two independently runnable reproductions covering ordinary XXXL and accessibility5 active-day presentation, including both participant identities. Retain actual environment readback, explicit score/identity semantics, accessible day-state assertions, and fixture-only screenshots captured before the unsuppressed clipping audit. Run before the product change; a capture-only pass is not the regression assertion, and one failing size must not prevent the other from running.
3. Choose the smallest adaptive layout after independent design review. Implement only the shared day/chart presentation and necessary focused tests. Do not alter scores or reduce font sizes.
4. Run focused UI tests and inspect full relevant screenshots. Confirm the original clipping signal, visible full scores, day/date readability, participant identity, and unchanged semantics. If an audit remains RED, preserve and investigate it rather than guessing or ignoring nil-element issues.
5. Independently review spec and quality. Then run the full deterministic UI suite serially, followed by the appropriate application/Core/Release/secret/privacy integration matrix. Freeze package resolution, use one fixture Simulator and one bounded scratch root, and keep XCTest clone count at zero.
6. Conventionally commit only reviewed logical changes and integrate through exact-head CI. Update the release ledger accurately about source identity; do not transfer unrelated physical/staging gate claims to a new binary.

## Review and current evidence

The focused repaired day cases and unavailable-source case passed. The expanded appearance matrix passed all twenty-eight audit checkpoints, and the full creation/history/anonymization/rematch/archive fixture flow passed. Independent review identified and then cleared two corrections: raw creation/rematch tokens are now rejected across labels, values, and identifiers from one snapshot (routing fixture UUID identifiers remain allowed); the current stacked day visibly says Today. Both day regressions and the strengthened creation/rematch flow passed together in `ResultSizeCaptureAndReviewCorrections.xcresult`.

That six-test run was five passed/one failed overall, not fully green: two capture-only result cases passed and the AX5 capture helper failed its strict positioning guard. A corrected, bounded helper subsequently captured the complete AX5 result in two overlapping segments without gaps; both unfiltered sequence-comparison audits still failed. The newer-runtime comparison also failed. The original unfiltered result Dynamic Type audit remains unresolved. No source in `CompetitionResultView.swift`, score/auth/server behavior, or selected phone artifact has changed.

Current cleanup status, September 4: all temporary diagnostic tests/helpers and diagnostic-only configuration assertion have been removed from the repository. Twenty-two permanent tests remain. The extra disposable iOS26 comparison Simulator was deleted; no installed runtime or authenticated staging state was removed. The complete cleaned-source suite finished in `FullUIPostLayoutRepair.xcresult`: **21 passed, 1 failed, 0 skipped**, in 1,040 seconds. The sole failure is `testRemoteCompetitionDynamicTypeRemainsReadable`; the full twenty-eight-checkpoint appearance matrix, day regressions, privacy checks, and existing lifecycles passed together. Final independent source/test review found no new correctness issue beyond that known unfiltered audit failure. The reviewed four-file diff and package lock were unchanged after the run. No build/XCTest process or XCTest clone remained, and both retained HealthComp Simulators were shut down. Integration remains on hold; no commit, merge, or production-readiness claim is made.

The broader rollout remains incomplete: real two-account scoring/lifecycle, selected physical background/APNs/auth/App Attest/deletion/replacement evidence, recovery rehearsal, and production promotion still require their own evidence and prerequisites.

## Temporary minimal result probe continuation

The next diagnostic compares four DEBUG-only synthetic score cards: the existing adaptive layout versus an always-stacked layout, each with and without the existing single spoken accessibility label. Font styles, scores, names, and card styling remain identical across these controls. Each independent test asserts actual Light/large environment and diagnostic mode, checks its exposed content, saves a synthetic screenshot, and runs the unfiltered Dynamic Type audit. No preferred-size override or audit exemption is introduced. Run only these four cases, serially, in the existing disposable iOS18.4 fixture Simulator and scratch directory. Remove all marked temporary code before integration. A control passing is not evidence that the actual result screen is fixed. No physical phone, authenticated staging, hosted write, production, credential, or package-resolution action is in scope.

The four controls completed after one separately recorded runner-startup failure: both adaptive cases failed, both stacked cases passed. Grouped and ungrouped failure crops identified points and the full opponent name respectively. Next, compare conditional HStack/VStack with identity-preserving AnyLayout at an identical temporary switch above the large category, retaining grouping, spacing, native fonts, and content. This is a matched diagnostic policy, not a new product category threshold. The two independent tests use the unchanged actual-configuration/content/screenshot/unfiltered-audit helper. Only these two cases are run next. Do not infer a product fix or framework defect from this reduction alone.

The matched conditional/AnyLayout pair passed 2/2. A further temporary measured-layout control now replaces only the fitting mechanism: measure the native horizontal layout's unspecified ideal width, choose it when it fits the finite width proposal, otherwise delegate to the native vertical layout. Nil/infinite width keeps the horizontal ideal/max query; both sizing and placement use the same original proposal and a fresh matching built-in cache. No category threshold, font shrink, content change, accessibility change, or animation is added. It keeps one owner subtree and remains DEBUG-only. Run its single unfiltered diagnostic before considering any production change. The old production view and the permanent 21/22 suite status are still unchanged.

The measured control passed 1/1. Independent review requested premeasurement of the fresh placement cache, which was applied before that run. Nonfinite-proposal behavior and actual flexible-name allocation still need qualification; this pass does not qualify the original screen. All temporary probes were then removed and preserved outside the repo, restoring the exact reviewed four-file diff SHA-256 `5901563912b23de55a24466a0c77202f35ad6e58a726b3de089b6cc02520b283`. Twenty-two permanent tests remain, and `CompetitionResultView.swift` is unchanged. Both retained Simulators are shut down, no Xcode/XCTest gate remains, and no product result fix, commit, merge, or production action is claimed. The next implementation step is edge-case validation and a focused real-screen RED-to-GREEN replacement of the fitting mechanism, preserving compact-when-fits behavior and all existing font/spoken contracts.

## Actual result-layout implementation sequence

Introduce one result-specific internal `CompetitionFinalScoreLayout` using the measured diagnostic's native horizontal/vertical delegates. First keep the actual result card on its original ViewThatFits while application-hosted tests exercise the candidate at zero, unspecified, infinite, mixed, narrow, and wide proposals. The tests use an actual SwiftUI layout under a finite UIKit host, not a handwritten approximation of layout behavior. Preserve the original production UI audit unchanged as the existing RED reproduction.

Finite/nonnegative geometry is a chosen invariant for this score component, not a claim that Apple's general Layout protocol forbids all infinite responses. If exploratory nonfinite proposals propagate invalid geometry, normalize only the nonfinite dimensions to unspecified before both delegate sizing and placement; leave finite widths and nil semantics intact. Do not clamp the returned size after measuring under a different proposal, invent a hard-coded screen width, or choose a category threshold. Warm the selected placement cache using the identical normalized proposal. Keep the helper result-specific and preserve every native font, score, name, separator, accessibility label/identifier, card style, and publication behavior.

After edge tests pass, replace only the final card's fitting container with this layout and run the original unfiltered Dynamic Type audit plus focused real-result readability/appearance coverage. Inspect actual compact and stacked screenshots. Then independent review and the complete automated integration matrix are required before a conventional commit and main integration. No phone, credentials, account, hosted state, signing, or production action is included in this implementation batch.
